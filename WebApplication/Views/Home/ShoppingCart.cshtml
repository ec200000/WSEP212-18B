@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
@{
    ViewData["Title"] = "Shopping Cart";
}
<!DOCTYPE html>

<html>
<body>
    @model WebApplication.Models.UserModel
    <div>
        @using (Html.BeginForm("TryShowShoppingCart", "Home", FormMethod.Post)){
            WSEP212.DomainLayer.ShoppingCart cart = HttpContextAccessor.HttpContext.Session.GetObject<WSEP212.DomainLayer.ShoppingCart>
               ("shopping_cart");
            //@Html.DisplayText(cart.shoppingBags[1].items[1].ToString());
                <button class="w-10 btn btn-lg btn-primary" type="submit">Purchase</button>
          }
        </div>
    </body>
</html>

@if (ViewBag.Alert != null)
{
    <script type="text/javascript">

            window.onload = function() {
                alert("@ViewBag.Alert");
            };
    </script>

}